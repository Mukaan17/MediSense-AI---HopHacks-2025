// Runtime configuration. This default file sets nothing, so the app falls
// back to build-time env / dev defaults. Deployments overwrite this file at
// container start (see docker/40-runtime-config.sh) - one build, any backend.
window.__MEDISENSE_CONFIG__ = {};
