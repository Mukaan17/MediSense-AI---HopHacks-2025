**/*.pyc
__pycache__/
.git/
.github/
.gitignore
venv_new/
.venv/
.env

# Large dataset archives
*.tar
*.tar.gz
*.zip

# Corpus files that never shipped (English Train.json is required: the
# entrypoint builds the RAG knowledge base from it)
English Dev.json
English Test.json
Synthetic English Train Data.json

# Local data and logs (the 31 KB imaging head checkpoint ships with the
# image; the BiomedCLIP backbone still downloads from HF at first load)
data/
logs/

# Frontend build artifacts
frontend/node_modules/
frontend/build/

# Datasets
chexpert/
