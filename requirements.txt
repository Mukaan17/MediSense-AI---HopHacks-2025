# ---------- Core math / arrays ----------
numpy==1.26.4

# ---------- PyTorch (CPU) ----------
torch==2.13.0
torchvision==0.28.0
torchaudio==2.11.0

# ---------- Vision / CLIP ----------
open_clip_torch==2.23.0
timm==0.9.16
pillow==10.2.0

# ---------- Data / ML tools ----------
pandas==2.2.2
scikit-learn==1.9.0

# ---------- API server ----------
fastapi==0.141.1
uvicorn==0.52.4
starlette==1.6.0
pydantic==2.13.4
python-multipart==0.0.32
python-dotenv==1.2.3
requests==2.34.2
websockets==12.0

# ---------- RAG stack ----------
chromadb==0.5.11
sentence-transformers==2.7.0
anthropic>=1.0
faster-whisper>=1.0.0
huggingface_hub==0.23.5
safetensors==0.8.0
accelerate==0.30.1

# ---------- LangChain ----------
langchain==0.2.14
langchain-core==0.2.34
langchain-community==0.4.2

# ---------- Audio / WhisperX ----------
librosa==0.10.1
soundfile==0.14.0
pyaudio==0.2.14
whisperx==3.2.0
openai-whisper==20231117

# ---------- Security utils ----------
python-jose[cryptography]>=3.5.0
bcrypt>=4.0
jinja2>=3.1
prometheus-client>=0.20
sentry-sdk[fastapi]>=2.0
arq>=0.26
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1

# ---------- Compatibility pins ----------
protobuf>=3.20.2,<5.0.0

# ---------- Optional (speaker diarization; requires HF token) ----------
pyannote.audio==3.1.1
pyannote.core==5.0.0


# vishak/Documents/Projects/HopHacks/requirements.lock
# uvicorn api.server:app --host 0.0.0.0 --port 8000
redis>=5.0
